// Simple block
{
    let blockVariable = "This is block variable value"
    console.log(blockVariable)
    blockVariable = "Modified block variable value"
    console.log(blockVariable)
  
    const constantVariable = "This is constant variable value"
    console.log(constantVariable)
  
    // constantVariable = "Unable to modify the constanct variable value"
  }
  
  // console.log(blockVariable) // not possible to access outside the block
  // console.log(constantVariable) // not possible to access outside the block
  